# Visual regression baselines

#

# Generated with Playwright Chromium (see playwright.config.ts snapshotPathTemplate).

# Update: pnpm run test:e2e -- e2e/visual.spec.ts --update-snapshots

# Prefer regenerating inside the official Playwright Docker image so CI (Linux) matches.
